# Backend configuration (copy to .env)

# Server
PORT=3000
NODE_ENV=development

# Auth
JWT_SECRET=change-me-super-long-random
JWT_EXPIRES_IN=1h

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=sewa_user
DB_PASSWORD=change-me
DB_NAME=sewa

# Optional seed (used by seeding script / migration if enabled)
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_EMAIL=superadmin@sewa.local
SUPERADMIN_PASSWORD=change-me
